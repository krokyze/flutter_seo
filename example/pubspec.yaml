name: seo_example

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  faker: ^2.1.0
  auto_route: ^6.1.0
  visibility_detector: ^0.4.0+2
  seo:
    path: ../

dev_dependencies:
  flutter_lints: ^2.0.1
  auto_route_generator: ^6.1.0
  build_runner: ^2.3.3
